# Подключаем движок Cucumber в JUnit Platform
junit.platform.discovery.include-engines = cucumber

# Где искать .feature-файлы
cucumber.features = classpath:features

# В каком пакете лежат шаги-степы
cucumber.glue = steps

# Плагины для вывода результатов
cucumber.plugin = pretty, summary